apiVersion: apps/v1
kind: Deployment
metadata:
  name: target
  namespace: test-agents
spec:
  replicas: 5
  selector:
    matchLabels:
      app: target
  template:
    metadata:
      labels:
        app: target
    spec:
      containers:
        - name: app
          image: pause:3.9
