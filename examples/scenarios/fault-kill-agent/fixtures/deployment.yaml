apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: test-agents
spec:
  replicas: 2
  selector:
    matchLabels:
      app: controller
  template:
    metadata:
      labels:
        app: controller
    spec:
      containers:
        - name: agent
          image: pause:3.9
