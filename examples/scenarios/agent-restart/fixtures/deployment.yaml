apiVersion: apps/v1
kind: Deployment
metadata:
  name: workload
  namespace: test-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workload
  template:
    metadata:
      labels:
        app: workload
    spec:
      containers:
        - name: app
          image: pause:3.9
